export type SubtitleCue = {
  cueIndex: number;
  startMs: number;
  endMs: number;
  text: string;
};

export type SubtitleExportCue = {
  cueIndex: number;
  startMs: number;
  endMs: number;
  text: string;
};
